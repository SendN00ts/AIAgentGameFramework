"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var __async = (__this, __arguments, generator) => {
  return new Promise((resolve, reject) => {
    var fulfilled = (value) => {
      try {
        step(generator.next(value));
      } catch (e) {
        reject(e);
      }
    };
    var rejected = (value) => {
      try {
        step(generator.throw(value));
      } catch (e) {
        reject(e);
      }
    };
    var step = (x) => x.done ? resolve(x.value) : Promise.resolve(x.value).then(fulfilled, rejected);
    step((generator = generator.apply(__this, __arguments)).next());
  });
};

// src/index.ts
var index_exports = {};
__export(index_exports, {
  Chat: () => Chat,
  ChatAgent: () => ChatAgent,
  ExecutableGameFunctionResponse: () => ExecutableGameFunctionResponse,
  ExecutableGameFunctionStatus: () => ExecutableGameFunctionStatus,
  GameAgent: () => agent_default,
  GameFunction: () => function_default,
  GameWorker: () => worker_default,
  LLMModel: () => LLMModel
});
module.exports = __toCommonJS(index_exports);

// src/api.ts
var import_axios = __toESM(require("axios"));
var GameClient = class {
  constructor(apiKey, llmModel) {
    this.apiKey = apiKey;
    this.llmModel = llmModel;
    this.client = null;
    this.runnerUrl = "https://game.virtuals.io";
  }
  init() {
    return __async(this, null, function* () {
      const accessToken = yield this.getAccessToken();
      this.client = import_axios.default.create({
        baseURL: this.runnerUrl,
        headers: {
          "Content-Type": "application/json",
          Authorization: `Bearer ${accessToken}`,
          model_name: this.llmModel
        }
      });
    });
  }
  getAccessToken() {
    return __async(this, null, function* () {
      const result = yield import_axios.default.post(
        "https://api.virtuals.io/api/accesses/tokens",
        {},
        {
          headers: {
            "x-api-key": this.apiKey
          }
        }
      );
      return result.data.data.accessToken;
    });
  }
  post(url, data) {
    return __async(this, null, function* () {
      yield this.init();
      if (!this.client) {
        throw new Error("Client is not initialized");
      }
      const result = yield this.client.post("/prompts", {
        data: {
          method: "post",
          headers: {
            "Content-Type": "application/json"
          },
          route: url,
          data
        }
      });
      return result.data;
    });
  }
  createMap(workers) {
    return __async(this, null, function* () {
      const result = yield this.post("/v2/maps", {
        locations: workers.map((worker) => ({
          id: worker.id,
          name: worker.name,
          description: worker.description
        }))
      });
      return result.data;
    });
  }
  createAgent(name, goal, description) {
    return __async(this, null, function* () {
      const result = yield this.post("/v2/agents", {
        name,
        goal,
        description
      });
      return result.data;
    });
  }
  getAction(agentId, mapId, worker, gameActionResult, environment, agentState) {
    return __async(this, null, function* () {
      const payload = {
        location: worker.id,
        map_id: mapId,
        environment,
        functions: worker.functions.map((fn) => fn.toJSON()),
        agent_state: agentState,
        version: "v2"
      };
      if (gameActionResult) {
        payload.current_action = gameActionResult;
      }
      const result = yield this.post(
        `/v2/agents/${agentId}/actions`,
        payload
      );
      return result.data;
    });
  }
  setTask(agentId, task) {
    return __async(this, null, function* () {
      const result = yield this.post(
        `/v2/agents/${agentId}/tasks`,
        {
          task
        }
      );
      return result.data.submission_id;
    });
  }
  getTaskAction(agentId, submissionId, worker, gameActionResult, environment) {
    return __async(this, null, function* () {
      const payload = {
        environment,
        functions: worker.functions.map((fn) => fn.toJSON())
      };
      if (gameActionResult) {
        payload.action_result = gameActionResult;
      }
      const result = yield this.post(
        `/v2/agents/${agentId}/tasks/${submissionId}/next`,
        payload
      );
      return result.data;
    });
  }
};
var api_default = GameClient;

// src/apiV2.ts
var import_axios2 = __toESM(require("axios"));
var GameClientV2 = class {
  constructor(apiKey, llmModel, llmModelBaseUrl, llmModelApiKey) {
    this.apiKey = apiKey;
    this.llmModel = llmModel;
    this.llmModelBaseUrl = llmModelBaseUrl;
    this.llmModelApiKey = llmModelApiKey;
    this.baseUrl = "https://sdk.game.virtuals.io/v2";
    this.client = import_axios2.default.create({
      baseURL: this.baseUrl,
      headers: {
        "Content-Type": "application/json",
        "x-api-key": this.apiKey,
        model_name: this.llmModel,
        model_base_url: this.llmModelBaseUrl,
        model_api_key: this.llmModelApiKey
      }
    });
  }
  createMap(workers) {
    return __async(this, null, function* () {
      const result = yield this.client.post("/maps", {
        data: {
          locations: workers.map((worker) => ({
            id: worker.id,
            name: worker.name,
            description: worker.description
          }))
        }
      });
      return result.data.data;
    });
  }
  createAgent(name, goal, description) {
    return __async(this, null, function* () {
      const result = yield this.client.post("/agents", {
        data: {
          name,
          goal,
          description
        }
      });
      return result.data.data;
    });
  }
  getAction(agentId, mapId, worker, gameActionResult, environment, agentState) {
    return __async(this, null, function* () {
      const payload = {
        location: worker.id,
        map_id: mapId,
        environment,
        functions: worker.functions.map((fn) => fn.toJSON()),
        agent_state: agentState,
        version: "v2"
      };
      if (gameActionResult) {
        payload.current_action = gameActionResult;
      }
      const result = yield this.client.post(
        `/agents/${agentId}/actions`,
        {
          data: payload
        }
      );
      return result.data.data;
    });
  }
  setTask(agentId, task) {
    return __async(this, null, function* () {
      const result = yield this.client.post(
        `/agents/${agentId}/tasks`,
        {
          data: { task }
        }
      );
      return result.data.data.submission_id;
    });
  }
  getTaskAction(agentId, submissionId, worker, gameActionResult, environment) {
    return __async(this, null, function* () {
      const payload = {
        environment,
        functions: worker.functions.map((fn) => fn.toJSON())
      };
      if (gameActionResult) {
        payload.action_result = gameActionResult;
      }
      const result = yield this.client.post(
        `/agents/${agentId}/tasks/${submissionId}/next`,
        {
          data: payload
        }
      );
      return result.data.data;
    });
  }
  createChat(data) {
    return __async(this, null, function* () {
      const response = yield this.client.post("/conversation", { data });
      const chatId = response.data.data.conversation_id;
      if (!chatId) {
        throw new Error("Agent did not return a conversation_id for the chat.");
      }
      return chatId;
    });
  }
  updateChat(conversationId, data) {
    return __async(this, null, function* () {
      const response = yield this.client.post(
        `/conversation/${conversationId}/next`,
        { data }
      );
      return response.data.data;
    });
  }
  reportFunction(conversationId, data) {
    return __async(this, null, function* () {
      const response = yield this.client.post(
        `/conversation/${conversationId}/function/result`,
        { data }
      );
      return response.data.data;
    });
  }
  endChat(conversationId, data) {
    return __async(this, null, function* () {
      const response = yield this.client.post(
        `/conversation/${conversationId}/end`,
        { data }
      );
      return response.data.data;
    });
  }
};
var apiV2_default = GameClientV2;

// src/interface/GameClient.ts
var LLMModel = /* @__PURE__ */ ((LLMModel2) => {
  LLMModel2["Llama_3_1_405B_Instruct"] = "Llama-3.1-405B-Instruct";
  LLMModel2["Llama_3_3_70B_Instruct"] = "Llama-3.3-70B-Instruct";
  LLMModel2["DeepSeek_R1"] = "DeepSeek-R1";
  LLMModel2["DeepSeek_V3"] = "DeepSeek-V3";
  LLMModel2["Qwen_2_5_72B_Instruct"] = "Qwen-2.5-72B-Instruct";
  return LLMModel2;
})(LLMModel || {});

// src/agent.ts
var GameAgent = class _GameAgent {
  constructor(apiKey, options) {
    this.agentId = null;
    this.mapId = null;
    this.gameActionResult = null;
    const llmModel = options.llmModel || "Llama-3.1-405B-Instruct" /* Llama_3_1_405B_Instruct */;
    const llmModelBaseUrl = options.llmModelBaseUrl || "";
    const llmModelApiKey = options.llmModelApiKey || "";
    if (options.llmModelBaseUrl || options.llmModelApiKey) {
      if (!options.llmModelBaseUrl || !options.llmModelApiKey) {
        throw new Error(
          "Both llmModelBaseUrl and llmModelApiKey must be provided together"
        );
      }
    }
    this.gameClient = apiKey.startsWith("apt-") ? new apiV2_default(apiKey, llmModel, llmModelBaseUrl, llmModelApiKey) : new api_default(apiKey, llmModel);
    this.workerId = options.workers[0].id;
    this.name = options.name;
    this.goal = options.goal;
    this.description = options.description;
    this.workers = options.workers;
    this.getAgentState = options.getAgentState;
  }
  log(msg) {
    console.log(`[${this.name}] ${msg}`);
  }
  init() {
    return __async(this, null, function* () {
      const map = yield this.gameClient.createMap(this.workers);
      const agent = yield this.gameClient.createAgent(
        this.name,
        this.goal,
        this.description
      );
      this.workers.forEach((worker) => {
        worker.setAgentId(agent.id);
        worker.setLogger(this.log.bind(this));
        worker.setGameClient(this.gameClient);
      });
      this.mapId = map.id;
      this.agentId = agent.id;
    });
  }
  setLogger(logger) {
    this.log = (msg) => logger(this, msg);
  }
  getWorkerById(workerId) {
    const worker = this.workers.find((worker2) => worker2.id === workerId);
    if (!worker) {
      throw new Error("Worker not found");
    }
    return worker;
  }
  step(options) {
    return __async(this, null, function* () {
      if (!this.agentId || !this.mapId) {
        throw new Error("Agent not initialized");
      }
      const { verbose } = options || {};
      const worker = this.workers.find((worker2) => worker2.id === this.workerId);
      if (!worker) {
        throw new Error("Worker not found");
      }
      const environment = worker.getEnvironment ? yield worker.getEnvironment() : {};
      const agentState = this.getAgentState ? yield this.getAgentState() : {};
      if (verbose) {
        this.log(`Environment State: ${JSON.stringify(environment)}`);
        this.log(`Agent State: ${JSON.stringify(agentState)}`);
      }
      const action = yield this.gameClient.getAction(
        this.agentId,
        this.mapId,
        worker,
        this.gameActionResult,
        environment,
        agentState
      );
      (options == null ? void 0 : options.verbose) && this.log(`Action State: ${JSON.stringify(action.agent_state || {})}.`);
      this.gameActionResult = null;
      switch (action.action_type) {
        case "call_function" /* CallFunction */:
        case "continue_function" /* ContinueFunction */:
          verbose && this.log(
            `Performing function ${action.action_args.fn_name} with args ${JSON.stringify(action.action_args.args)}.`
          );
          const fn = worker.functions.find(
            (fn2) => fn2.name === action.action_args.fn_name
          );
          if (!fn) {
            throw new Error("Function not found");
          }
          const result = yield fn.execute(
            action.action_args.args,
            (msg) => this.log(msg)
          );
          verbose && this.log(`Function status [${result.status}]: ${result.feedback}.`);
          this.gameActionResult = result.toJSON(action.action_args.fn_id);
          break;
        case "go_to" /* GoTo */:
          this.workerId = action.action_args.location_id;
          verbose && this.log(`Going to ${action.action_args.location_id}.`);
          break;
        case "wait" /* Wait */:
          verbose && this.log(`No actions to perform.`);
          return action.action_type;
        default:
          return "unknown" /* Unknown */;
      }
      return action.action_type;
    });
  }
  run(heartbeatSeconds, options) {
    return __async(this, null, function* () {
      if (!this.agentId || !this.mapId) {
        throw new Error("Agent not initialized");
      }
      while (true) {
        const action = yield this.step({
          verbose: (options == null ? void 0 : options.verbose) || false
        });
        if (action === "wait" /* Wait */ || action === "unknown" /* Unknown */) {
          break;
        }
        yield new Promise(
          (resolve) => setTimeout(resolve, heartbeatSeconds * 1e3)
        );
      }
    });
  }
  save() {
    return {
      agentId: this.agentId,
      mapId: this.mapId,
      gameActionResult: this.gameActionResult
    };
  }
  initWorkers() {
    return __async(this, null, function* () {
      this.workers.forEach((worker) => {
        worker.setAgentId(this.agentId || "");
        worker.setLogger(this.log.bind(this));
        worker.setGameClient(this.gameClient);
      });
    });
  }
  static load(apiKey, name, goal, description, savedState, workers) {
    return __async(this, null, function* () {
      const agent = new _GameAgent(apiKey, {
        name,
        goal,
        description,
        workers
      });
      agent.agentId = savedState.agentId;
      agent.mapId = savedState.mapId;
      agent.gameActionResult = savedState.gameActionResult;
      return agent;
    });
  }
};
var agent_default = GameAgent;

// src/worker.ts
var GameWorker = class {
  constructor(options) {
    this.agentId = null;
    this.logger = null;
    this.gameClient = null;
    this.gameActionResult = null;
    this.id = options.id;
    this.name = options.name;
    this.description = options.description;
    this.functions = options.functions;
    this.getEnvironment = options.getEnvironment;
  }
  setAgentId(agentId) {
    this.agentId = agentId;
  }
  setLogger(logger) {
    this.logger = logger;
  }
  setGameClient(gameClient) {
    this.gameClient = gameClient;
  }
  step(submissionId, options) {
    return __async(this, null, function* () {
      var _a, _b, _c;
      if (!this.agentId) {
        throw new Error("Agent not initialized");
      }
      if (!this.gameClient) {
        throw new Error("Game client not initialized");
      }
      const environment = this.getEnvironment ? yield this.getEnvironment() : {};
      if (options == null ? void 0 : options.verbose) {
        (_a = this.logger) == null ? void 0 : _a.call(this, `Environment State: ${JSON.stringify(environment)}`);
      }
      const action = yield this.gameClient.getTaskAction(
        this.agentId,
        submissionId,
        this,
        this.gameActionResult,
        environment
      );
      this.gameActionResult = null;
      if (!["call_function" /* CallFunction */, "continue_function" /* ContinueFunction */].includes(
        action.action_type
      )) {
        return false;
      }
      const fn = this.functions.find(
        (fn2) => fn2.name === action.action_args.fn_name
      );
      if (!fn) {
        throw new Error("Function not found");
      }
      (options == null ? void 0 : options.verbose) && ((_b = this.logger) == null ? void 0 : _b.call(
        this,
        `Performing function ${action.action_args.fn_name} with args ${JSON.stringify(action.action_args.args)}.`
      ));
      const args = action.action_args.args;
      const result = yield fn.execute(args, (msg) => {
        var _a2;
        return (_a2 = this.logger) == null ? void 0 : _a2.call(this, msg);
      });
      (options == null ? void 0 : options.verbose) && ((_c = this.logger) == null ? void 0 : _c.call(this, `Function status: ${result.status} - ${result.feedback}.`));
      this.gameActionResult = result.toJSON(action.action_args.fn_id);
      return true;
    });
  }
  runTask(task, options) {
    return __async(this, null, function* () {
      if (!this.agentId) {
        throw new Error("Agent not initialized");
      }
      if (!this.gameClient) {
        throw new Error("Game client not initialized");
      }
      const submissionId = yield this.gameClient.setTask(this.agentId, task);
      while (true) {
        const result = yield this.step(submissionId, options);
        if (!result) {
          break;
        }
      }
    });
  }
};
var worker_default = GameWorker;

// src/function.ts
var ExecutableGameFunctionStatus = /* @__PURE__ */ ((ExecutableGameFunctionStatus2) => {
  ExecutableGameFunctionStatus2["Done"] = "done";
  ExecutableGameFunctionStatus2["Failed"] = "failed";
  return ExecutableGameFunctionStatus2;
})(ExecutableGameFunctionStatus || {});
var ExecutableGameFunctionResponse = class {
  constructor(status, feedback) {
    this.status = status;
    this.feedback = feedback;
  }
  toJSON(id) {
    return {
      action_id: id,
      action_status: this.status,
      feedback_message: this.feedback
    };
  }
};
var GameFunction = class {
  constructor(options) {
    this.name = options.name;
    this.description = options.description;
    this.args = options.args;
    this.executable = options.executable;
    this.hint = options.hint;
  }
  toJSON() {
    return {
      fn_name: this.name,
      fn_description: this.description,
      args: this.args,
      hint: this.hint
    };
  }
  execute(args, logger) {
    return __async(this, null, function* () {
      const argValues = Object.keys(args).reduce(
        (acc, key) => {
          var _a;
          acc[key] = (_a = args[key]) == null ? void 0 : _a.value;
          return acc;
        },
        {}
      );
      return yield this.executable(argValues, logger);
    });
  }
};
var function_default = GameFunction;

// src/chatAgent.ts
var Chat = class {
  constructor(chatId, client, actionSpace, getStateFn) {
    this.chatId = chatId;
    this.client = client;
    this.actionSpace = actionSpace ? Object.fromEntries(actionSpace.map((f) => [f.name, f])) : null;
    this.getStateFn = getStateFn || null;
  }
  next(message) {
    return __async(this, null, function* () {
      const convoResponse = yield this.updateConversation(message);
      let responseMessage;
      let functionCallResponse = null;
      if (convoResponse.function_call) {
        if (!this.actionSpace) {
          throw new Error("No functions provided");
        }
        const fnName = convoResponse.function_call.fn_name;
        const fnToCall = this.actionSpace[fnName];
        if (!fnToCall) {
          throw new Error(
            `Function ${fnName}, returned by the agent, not found in action space`
          );
        }
        const executableArgs = Object.entries(
          convoResponse.function_call.args
        ).reduce((acc, [key, value]) => {
          acc[key] = { value: String(value) };
          return acc;
        }, {});
        const result = yield fnToCall.execute(
          executableArgs,
          (msg) => console.log(msg)
        );
        responseMessage = yield this.reportFunctionResult(
          result,
          convoResponse.function_call.id
        );
        functionCallResponse = {
          fn_name: fnName,
          fn_args: convoResponse.function_call.args,
          result: {
            action_id: convoResponse.function_call.id,
            action_status: { value: result.status },
            feedback_message: result.feedback
          }
        };
      } else {
        responseMessage = convoResponse.message || "";
      }
      return {
        message: responseMessage,
        isFinished: convoResponse.is_finished,
        functionCall: functionCallResponse
      };
    });
  }
  end(message) {
    this.client.endChat(this.chatId, { message });
  }
  updateConversation(message) {
    return __async(this, null, function* () {
      const data = {
        message,
        state: this.getStateFn ? this.getStateFn() : null,
        functions: this.actionSpace ? Object.values(this.actionSpace).map((f) => f.toJSON()) : null
      };
      const result = yield this.client.updateChat(this.chatId, data);
      return result;
    });
  }
  reportFunctionResult(result, fnId) {
    return __async(this, null, function* () {
      const data = {
        fn_id: fnId,
        result: result.feedback ? `${result.status}: ${result.feedback}` : result.status
      };
      const response = yield this.client.reportFunction(this.chatId, data);
      if (!response.message) {
        throw new Error(
          "Agent did not return a message for the function report."
        );
      }
      return response.message;
    });
  }
};
var ChatAgent = class {
  constructor(api_key, prompt) {
    this._api_key = api_key;
    this.prompt = prompt;
    if (api_key.startsWith("apt-")) {
      this.client = new apiV2_default(api_key, "Llama-3.3-70B-Instruct" /* Llama_3_3_70B_Instruct */, "", "");
    } else {
      throw new Error("Please use V2 API key to use ChatAgent");
    }
  }
  createChat(data) {
    return __async(this, null, function* () {
      const chat_id = yield this.client.createChat({
        prompt: this.prompt,
        partner_id: data.partnerId,
        partner_name: data.partnerName
      });
      return new Chat(chat_id, this.client, data.actionSpace, data.getStateFn);
    });
  }
};
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  Chat,
  ChatAgent,
  ExecutableGameFunctionResponse,
  ExecutableGameFunctionStatus,
  GameAgent,
  GameFunction,
  GameWorker,
  LLMModel
});
